name: AAA

on:
  workflow_dispatch:
 
permissions:
  contents: read

jobs:
  job1:
    runs-on: ubuntu-latest
    name: Job 1
    steps:
      - name: Step 1
        run: |
          docker run --hostname pebble --rm -d -p 14000:14000 -p 15000:15000 ghcr.io/letsencrypt/pebble:latest
          
          
          
          wget --no-check-certificate https://pebble:14000/dir -O -
          
          php -v
      
