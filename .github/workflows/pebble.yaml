name: Build Pebble

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  PEBBLE_VERSION: "2.10.0"
  
jobs:
  build-pebble:
    name: Build Pebble Containers
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v7

      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Pebble EAB
        uses: docker/build-push-action@v7
        with:
          context: .
          file: test/pebble/Dockerfile
          build-args: |
            CONFIG=pebble-config-eab.json
          push: true
          tags: ghcr.io/${{ github.repository }}/pebble-eab:${{ env.PEBBLE_VERSION }}

      - name: Build Pebble non-EAB
        uses: docker/build-push-action@v7
        with:
          context: .
          file: test/pebble/Dockerfile
          build-args: |
            CONFIG=pebble-config-no-eab.json
          push: true
          tags: ghcr.io/${{ github.repository }}/pebble-no-eab:${{ env.PEBBLE_VERSION }}